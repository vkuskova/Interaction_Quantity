PLACEHOLDER: copy baselines_validation/per_seed.csv from your Drive (results/baselines_validation/) into this folder before the final tar. This artifact requires the R hierNet path and was not regenerable in the build environment.
